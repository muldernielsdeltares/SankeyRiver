{
  "name": "SankeyRiver",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/Sankey.ts --bundle --outfile=dist/sankey.js --format=esm",
    "build:min": "esbuild src/Sankey.ts --bundle --minify --outfile=dist/sankey.min.js --format=esm",
    "dev": "esbuild src/Sankey.ts --bundle --outfile=dist/sankey.js --format=esm --watch",
    "lint": "npx eslint"
  },
  "keywords": [],
  "author": "Niels Mulder",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "esbuild": "^0.25.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.47.0"
  }
}
